import { StyleSheet } from 'react-native';

const createStyles = () => {
  return StyleSheet.create({
    container: {
      marginTop: 20,
      margin: 12,
      flex: 1,
    },
  });
};

export default createStyles;
